name: Completrix Analysis

on: [push, pull_request]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Completrix
        uses: vireon/completrix-action@v1
        with:
          path: "."
          mode: "compile"
